# Check Alerting

To use this, you need to create `.env` file and set the environment variables in there. See example file `.env.example`.

Also you need to set user alerting emails in `alert_user_emails.json`